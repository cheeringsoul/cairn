// Placeholder. Real integration tests should live under integration_test/
// so they can reach the sqlite3 native libs. Unit-testing CairnApp directly
// from here pulls in drift → sqlite3 → dart:ffi, which isn't available in
// the `flutter test` headless runner.
void main() {}
